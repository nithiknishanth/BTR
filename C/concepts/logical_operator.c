#include<stdio.h>

int main(){
    //logical operators are used in logical operations

    // && logical operator ch
 
    float temp = 1000;
    if(temp >= 0 && temp <= 30 ){
        printf("\n The weather is good");

    }else{
        printf("\nThe Weather is bad");
    }
}
